"""Transliteration pairing collector scaffolding.

TODO(human): native speaker must produce matched native/romanized sentence pairs.
"""


def pair_transliterations(native_path: str, romanized_path: str) -> None:
    raise NotImplementedError(
        "TODO(human): implement transliteration pairing once human-validated data exists"
    )
