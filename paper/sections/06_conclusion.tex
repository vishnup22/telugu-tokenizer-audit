\section{Conclusion}

We present a reproducible audit pipeline for Telugu tokenizer fertility and
script fairness. Future work may evaluate vocabulary augmentation or
Telugu-aware pre-tokenization as mitigations.

Limitations include whitespace-based word counting, corpus representativeness,
and dependence on gated tokenizer access for some models.
